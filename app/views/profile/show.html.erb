<h1 class="text-center">My Profile</h1>
<div class="profile-header-wrapper">
  <div class="container profile-header-container text-center">
    <div class="profile-header-infos">
      <img src="images/papillard.jpeg" alt="">
      <h1>
        <% if current_user.username.nil? %>
        <%= current_user.first_name %>
        <%= current_user.last_name %>
        <% else %>
        <%= current_user.username %>
        <% end %>
      </h1>
      <p>
        <%= current_user.email %>
        <br>
        <%= current_user.phone_number %>
        <br>
      </p>
    </div>
  </div>
</div>







<div class="container">
  <div class="wrapper">
    <div class="row">
      <div class="col-md-6">

      </div>

      <div class="col-md-6" >

      </div>
    </div>

    <div class="row no-margin">
      <div class="col-md-12" style=" margin: 20px 0px; padding: 0px">
        <div class="profile-header-wrapper">
          <div class="profile-header-container text-center" style="padding: 0px;">
            <div class="tabs">
              <a class="tab active" data-target="#request">
                <h3>My requests</h3>
              </a>
              <a class="tab" data-target="#venue">
                <h3>My places</h3>
              </a>
              <a class="tab" data-target="#requestout">
                <h3>People requests</h3>
              </a>
            </div>
          </div>
        </div>
        <div class="tab-content" id="request" style="height: 250px; overflow:auto">
          <div class="content">

            <!--  -->

            <h1>JOE STATUS</h1>

            <% current_user.venues.each do |venue| %>
            <li><%= link_to venue.title, venue_path(venue.id), style: 'text-decoration: none; color:black;'  %></li>
            ###########Confirmed Bookings####################
            <% current_user.venues.each do |venue| %>
            <% status = venue.appointments.where(status: "Confirmed") %>
            <% status.each do |appointment| %>
            <ul>
              <li>
                <h4>
                  <%= link_to appointment.artwork.title, artwork_path(appointment.artwork_id), style: 'text-decoration: none; color:black;' %>,
                  <% if Artwork.find(appointment.artwork_id).owner.username.nil? %>
                  by <%= Artwork.find(appointment.artwork_id).owner.first_name%>
                  by <%= Artwork.find(appointment.artwork_id).owner.last_name%>
                  <% else %>
                  by <%= Artwork.find(appointment.artwork_id).owner.username%>
                  <% end %>
                  (<%= appointment.artwork.category %>)
                  <br>
                  Status: <%= appointment.status %>
                  <br>
                </h4>
                For your: <%= appointment.venue.title %>
                <br>
                From <%= appointment.start_date %>
                to <%= appointment.end_date %>
                <br>
                Email: <%= Artwork.find(appointment.artwork_id).owner.email%>
                <br>
                <% if Artwork.find(appointment.artwork_id).owner.email.nil? %>
                <% else %>
                Phone number: <%= Artwork.find(appointment.artwork_id).owner.phone_number%>
                <% end %>
                <br>
              </li>
            </ul>
            <% end %>
            <% end %>
            ###############################
            ###########Declined Bookings####################
            <% current_user.venues.each do |venue| %>
            <% status = venue.appointments.where(status: "Declined") %>
            <% status.each do |appointment| %>
            <ul>
              <li>
                <h4>
                  <%= link_to appointment.artwork.title, artwork_path(appointment.artwork_id), style: 'text-decoration: none; color:black;' %>,
                  <% if Artwork.find(appointment.artwork_id).owner.username.nil? %>
                  by <%= Artwork.find(appointment.artwork_id).owner.first_name%>
                  by <%= Artwork.find(appointment.artwork_id).owner.last_name%>
                  <% else %>
                  by <%= Artwork.find(appointment.artwork_id).owner.username%>
                  <% end %>
                  (<%= appointment.artwork.category %>)
                  <br>
                  Status: <%= appointment.status %>
                  <br>
                </h4>
                For your: <%= appointment.venue.title %>
                <br>
                From <%= appointment.start_date %>
                to <%= appointment.end_date %>
                <br>
                Email: <%= Artwork.find(appointment.artwork_id).owner.email%>
                <br>
                <% if Artwork.find(appointment.artwork_id).owner.email.nil? %>
                <% else %>
                Phone number: <%= Artwork.find(appointment.artwork_id).owner.phone_number%>
                <% end %>
                <br>
                <%= link_to "Delete", appointment_path(appointment), method: :delete, data: { confirm: "Are you sure?"}, class: "btn-book-show" %>
              </li>
            </ul>
            <% end %>
            <% end %>
            ###############################
            ###########Pending Apporval Bookings####################
            <% current_user.venues.each do |venue| %>
            <% status = venue.appointments.where(status: "Pending") %>
            <% status.each do |appointment| %>
            <ul>
              <li>
                <h4>
                  <%= link_to appointment.artwork.title, artwork_path(appointment.artwork_id), style: 'text-decoration: none; color:black;' %>,
                  <% if Artwork.find(appointment.artwork_id).owner.username.nil? %>
                  by <%= Artwork.find(appointment.artwork_id).owner.first_name%>
                  by <%= Artwork.find(appointment.artwork_id).owner.last_name%>
                  <% else %>
                  by <%= Artwork.find(appointment.artwork_id).owner.username%>
                  <% end %>
                  (<%= appointment.artwork.category %>)
                  <br>
                  Status: <%= appointment.status %>
                  <br>
                </h4>
                For your: <%= appointment.venue.title %>
                <br>
                From <%= appointment.start_date %>
                to <%= appointment.end_date %>
                <br>
                Email: <%= Artwork.find(appointment.artwork_id).owner.email%>
                <br>
                <% if Artwork.find(appointment.artwork_id).owner.email.nil? %>
                <% else %>
                Phone number: <%= Artwork.find(appointment.artwork_id).owner.phone_number%>
                <% end %>
                <br>
              </li>
            </ul>
            <% end %>
            <% end %>
            <% end %>

            <!--  -->

            <div class="content-group">
              <strong>
                Dimensions:
              </strong>
              <div class="values">

              </div>
            </div>
            <div class="content-group">
              <strong>
                Weight:
              </strong>
              <div class="values">
                dddddd
              </div>
            </div>
            <div class="content-group">
              <strong>
                City:
              </strong>
              <div class="values">
               ssssss
             </div>
           </div>
           <h3>
            Title jrqiogj
          </h3>

        </div>

      </div>
      <div class="tab-content hidden" id="venue" style="height: 250px; overflow:auto">
        <p>
          <% current_user.venues.each do |venue| %>
          <li><%= link_to venue.title, venue_path(venue.id), style: 'text-decoration: none; color:black;'  %></li>
          <% end %>
        </ul>
        <%= link_to "Create Venues", new_venue_path, style: 'text-decoration: none; color:black;' %>
      </p>
    </div>
    <div class="tab-content hidden" id="requestout" style="height: 250px; overflow:auto;">
      <div class="content" >

        <!--  -->
        <%  current_user.artworks.each do |artwork|  %>
        <% artwork.appointments.each do |appointment| %>
        <ul>
          <li>
            <% if appointment.venue.employees.first.username.nil? %>
            <%= appointment.venue.employees.first.first_name %>
            <%= appointment.venue.employees.first.last_name %>
            <% else %>
            <%= appointment.venue.employees.first.username %>
            <% end %>
            requested "<%= appointment.artwork.title %>"
            for <%= link_to appointment.venue.title,  venue_path(appointment.venue.id), style: 'text-decoration: none; color:black;'  %> <br>
            From <%= appointment.start_date%> to <%= appointment.end_date %>
            <br>
            <br>
            <%= link_to 'Confirm',  appointment_confirm_path(appointment), class: "btn btn-success" %>
            <%= link_to 'Decline',  appointment_decline_path(appointment), class: "btn btn-danger" %>
            <br>
            <br>
          </li>
        </ul>
        <% end %>
        <% end %>
        <!--  -->

        <h3>
          Title jrqiogj
        </h3>
        <p>
          Lorem ipsum dolor sit amet, consectetur adipisicing elit. Odio quo soluta incidunt iste nam? Nobis aliquid quam adipisci, ducimus, alias quo inventore vitae nam a
          <br><br>
          voluptates autem minus odio non quos quaerat nisi hic repudiandae odit ad similique asperiores, eius quisquam ea quas amet? Beatae impedit eaque facilis dolore vero consectetur voluptatibus velit veniam qui, asperiores sapiente voluptatem sed, repellat libero neque magni, delectus maiores at hic,
          <br><br>
          architecto laborum tempore! Repudiandae ut earum enim praesentium. Voluptates alias quos perferendis ducimus, laboriosam omnis fuga dolore ut voluptas? Blanditiis est nulla adipisci odio tempore excepturi cum asperiores eveniet corporis. A, fugit, optio.
        </p>
      </div>
    </div>
  </div>
</div>
<div class="row no-margin">
  <div class="col-md-12" style=" margin: 10px 0px;">
    <div class="text-center">
      <h3>My artworks</h3>
      <br>
    </div>
  </div>

  <div class="">
    <div class="col-md-12">
      <div class="inline-block-center">
        <div class="multiple-items">
          <%# artist = @artwork.owner %>
          <% art = Artwork.all %>
          <% if art.where(user_id: current_user).nil? %>
          nothing
          <% else %>
          <% collection = art.where(user_id: current_user) %>
          <% collection.each do |piece| %>
          <% if piece.photo.nil? %>
          <% else %>
          <% picture = piece.photo.path %>
          <div class="card-link card-pic">
            <%= link_to artwork_path(piece) do %>
            <div class="card-wrapper">
              <p class="card-text text-center"><%= piece.title %></p>
              <img class="card-image text-center" src="<%= cl_image_path(picture, width: 300, crop: "scale") %>" alt="sample image">
            </div>
            <% end %>
          </div>
          <% end %>
          <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="text-center">
    <%= link_to "Create artwork", new_artwork_path, class: "btn btn-primary", style: 'margin-top: 18px;' %>
  </div>
</div>
</div>
</div>
<br><br>
